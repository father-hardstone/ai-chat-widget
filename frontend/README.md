Documentation for this project is in the [repository README](../README.md) at the repo root (setup, API URL, run, build, scripts).

Commands like `npm run dev` are run from this `frontend/` directory after `cd frontend`.

# Required. Full origin of the chat API (scheme + host + port), no trailing slash.
# Local backend (see backend/.env PORT, default 3001):
VITE_API_BASE_URL=http://localhost:3001
#
# Production (set in Vercel / CI at build time):
# VITE_API_BASE_URL=https://your-api.example.com

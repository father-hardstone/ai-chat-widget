# Optional. Full origin of the chat API (no trailing slash).
# Leave unset to use same-origin /api/* — in dev, Vite proxies /api to the backend (see vite.config.ts).
# Set for split local dev or production when the API is on another host:
# VITE_API_BASE_URL=http://localhost:3001
